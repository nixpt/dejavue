# Patterns


## 2026-06-06T16:18:09-05:00

New event-type commands follow the trap/invariant/pattern mold: DEJAVUE_DIR.mkdir + write header if missing, append to <name>.md, append_event{event:<name>}, then wire into context doc loop + FTS sources/rebuild_fts + .gitattributes merge=union + all 3 embedded completions (regenerate) + a test mirroring 116/129.
